{
  "name": "movie-project-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "server": "^1.0.0",
    "web": "^1.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
